# dependencies / build
node_modules/
**/node_modules/
.next/
**/.next/
out/
*.tsbuildinfo
next-env.d.ts

# env
.env
.env*.local

# playwright browsers (large, installed locally)
.ms-playwright/

# heavy dev-only capture artifacts (the production build uses azure-clone-next/content-kit instead)
clone-kit/public/
clone-kit/screenshots/
clone-kit/content.bak/
verify/**/*.png
verify/**/*.jpeg

# misc
*.log
.DS_Store
.vercel
tmp/

# windows reserved device-name files (block accidental > NUL redirects)
NUL
**/NUL

# npm cache accidentally created in project dir — never commit
.npm-cache/
**/.npm-cache/
.npm/
